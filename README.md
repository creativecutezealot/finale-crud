## Project setup

### Node.js Server
```
cd node-js-server
node index.js
```

### Angular Client
```
cd angular-10-client
```
Run `npm install`, `npm start` for a dev server. Navigate to `http://localhost:4200/`.
